<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Evote</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="librerias/scss/estilos.css">
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
</head>
<body>
    <header>
        <div class="cabecera">
        <img src="images/logo-unsa.svg" alt="UNSA" class="logo">
            <div class="titulo">
                <h1><span class="text-big">Elecciones 2020</span></h1>
                <!--div id="txt"></div-->
                <span>Sistema Digital de Votación<b>E-vote</b></span>
            </div>
        </div>
    </header>
    <main>
        <!-- inicio formulario busqueda -->
        <div class="container">
        <div class="vertical-center">
        <form class="formulario" method="post" action="/login" onsubmit="return validateForm()">
            <fieldset>
                <legend>Identificación del Elector</legend>
                <div align="center">
                    <a href="/auth/google"><img src="images/login_unsa.png"  class="img-fluid"/></a>
                </div>
                             
                <div style="font-size: 16px;"> SI USTED NO RECUERDA SU CORREO UNSA POR FAVOR COMUNICARSE A lizardo@unsa.edu.pe o jhuajardo@unsa.edu.pe </div>
              

            </fieldset>
        </form>
        </div>
        </div>
    </main>
</body>
</html>

<script>
    /*
    function validateForm(){
      console.log("validating form");
      return true;
    }

    function startTime() {
        var today = new Date();
        var h = today.getHours();
        var m = today.getMinutes();
        var s = today.getSeconds();
        m = checkTime(m);
        s = checkTime(s);
        document.getElementById('txt').innerHTML =
        h + ":" + m + ":" + s;
        var t = setTimeout(startTime, 500);
    }
    function checkTime(i) {
        if (i < 10) {i = "0" + i};  // add zero in front of numbers < 10
        return i;
    }*/

</script>
